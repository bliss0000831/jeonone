/**
 * @deprecated `@gwangjang/platform` 으로 직접 import 권장.
 *
 * 이 파일은 M4 마이그레이션 호환 shim — 기존 `@/lib/native` import 깨지지 않게 유지.
 * 점진적으로 consumer 들이 `@gwangjang/platform` 으로 옮기면 이 shim 삭제.
 */
export * from "@gwangjang/platform"
