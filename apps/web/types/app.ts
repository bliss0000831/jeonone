/**
 * @deprecated `@gwangjang/types/app` 에서 직접 import 권장.
 * M6 호환 shim — 다수의 파일이 아직 이 경로를 사용하므로 유지합니다.
 * 새 코드에서는 `import { ... } from "@gwangjang/types/app"` 를 사용하세요.
 */
export * from "@gwangjang/types/app"
